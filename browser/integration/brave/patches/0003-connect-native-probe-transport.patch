diff --git a/browser/brave_content_browser_client.cc b/browser/brave_content_browser_client.cc
index 092130bf4..0f04044ab 100644
--- a/browser/brave_content_browser_client.cc
+++ b/browser/brave_content_browser_client.cc
@@ -92,3 +92,5 @@
+#include "brave/components/reverse_engineering_browser/browser/native_probe_session.h"
+#include "brave/components/reverse_engineering_browser/browser/native_probe_socket_client.h"
 #include "brave/components/request_otr/common/buildflags/buildflags.h"
 #include "brave/components/skus/common/features.h"
 #include "brave/components/skus/common/skus_internals.mojom.h"
@@ -565,4 +567,6 @@ void BraveContentBrowserClient::RenderProcessWillLaunch(
     content::RenderProcessHost* host) {
+  static_cast<void>(
+      reb::NativeProbeSocketClient::Get().StartFromCommandLine());
   Profile* profile = Profile::FromBrowserContext(host->GetBrowserContext());
   // The BraveRendererUpdater might be null for some irregular profiles, e.g.
   // the System Profile.
@@ -573,6 +575,18 @@ void BraveContentBrowserClient::RenderProcessWillLaunch(
   ChromeContentBrowserClient::RenderProcessWillLaunch(host);
 }
 
+void BraveContentBrowserClient::BindHostReceiverForRenderer(
+    content::RenderProcessHost* render_process_host,
+    mojo::GenericPendingReceiver receiver) {
+  if (auto probe_receiver =
+          receiver.As<reb::mojom::NativeProbeHost>()) {
+    reb::NativeProbeSession::Get().BindHost(std::move(probe_receiver));
+    return;
+  }
+  ChromeContentBrowserClient::BindHostReceiverForRenderer(
+      render_process_host, std::move(receiver));
+}
+
 void BraveContentBrowserClient::
     RegisterAssociatedInterfaceBindersForRenderFrameHost(
         content::RenderFrameHost& render_frame_host,                // NOLINT
diff --git a/browser/brave_content_browser_client.h b/browser/brave_content_browser_client.h
index 9604f7719..abf012250 100644
--- a/browser/brave_content_browser_client.h
+++ b/browser/brave_content_browser_client.h
@@ -57,5 +57,8 @@ class BraveContentBrowserClient : public ChromeContentBrowserClient {
   void BrowserURLHandlerCreated(content::BrowserURLHandler* handler) override;
   void RenderProcessWillLaunch(content::RenderProcessHost* host) override;
+  void BindHostReceiverForRenderer(
+      content::RenderProcessHost* render_process_host,
+      mojo::GenericPendingReceiver receiver) override;
   void RegisterAssociatedInterfaceBindersForRenderFrameHost(
       content::RenderFrameHost& render_frame_host,
       blink::AssociatedInterfaceRegistry& associated_registry) override;
diff --git a/renderer/brave_content_renderer_client.cc b/renderer/brave_content_renderer_client.cc
index 6c6b8d934..1b22d623a 100644
--- a/renderer/brave_content_renderer_client.cc
+++ b/renderer/brave_content_renderer_client.cc
@@ -18,5 +18,6 @@
 #include "brave/components/misc_metrics/renderer/web3_metrics_render_frame_observer.h"
 #include "brave/components/playlist/core/common/buildflags/buildflags.h"
+#include "brave/components/reverse_engineering_browser/renderer/native_probe_transport.h"
 #include "brave/components/safe_builtins/renderer/safe_builtins.h"
 #include "brave/components/script_injector/renderer/script_injector_render_frame_observer.h"
 #include "brave/components/skus/common/features.h"
@@ -141,6 +142,10 @@ BraveContentRendererClient::~BraveContentRendererClient() = default;
 
 void BraveContentRendererClient::RenderThreadStarted() {
   ChromeContentRendererClient::RenderThreadStarted();
+  mojo::PendingRemote<reb::mojom::NativeProbeHost> pending_probe_host;
+  content::RenderThread::Get()->BindHostReceiver(
+      pending_probe_host.InitWithNewPipeAndPassReceiver());
+  reb::NativeProbeTransport::Get().Connect(std::move(pending_probe_host));
 
   brave_observer_ = std::make_unique<BraveRenderThreadObserver>();
   content::RenderThread::Get()->AddObserver(brave_observer_.get());
